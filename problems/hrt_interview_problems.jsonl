{"name":"Random Circle Chords","tag":["geometry"],"statements":"Randomly choose $n$ points $X_1,\\dots,X_n$ on a circle. Draw the chords $X_1X_2, X_2X_3, \\dots, X_nX_1$. Intersections on the boundary do not count. What is the expected number of intersections strictly inside the circle?","hint":"Only pairs of non-adjacent chords can intersect. For four random endpoints on a circle, exactly one of the three pairings crosses.","solution":"There are $n$ chords. Adjacent chord pairs share an endpoint and cannot create an interior intersection, so the number of non-adjacent chord pairs is $\\binom n2-n=n(n-3)/2$. For any non-adjacent pair, the four endpoints are in random cyclic order and the two chords cross with probability $1/3$. Therefore the expected number of interior intersections is $$\\boxed{\\frac{n(n-3)}{6}}.$$"}
{"name":"Height Waiting Time","tag":["expectation"],"statements":"People arrive with i.i.d. continuous heights. After observing the first person, how many additional people do you expect to observe until seeing someone taller than the first person? What is the median of this waiting time?","hint":"Condition on the first person's quantile $U=F(X_0)$, then mix a geometric random variable over $U\\sim U(0,1)$.","solution":"Let the first person's height quantile be $U\\sim U(0,1)$. Conditional on $U=u$, each later person is taller with probability $1-u$, so the additional waiting time $T$ is geometric. Thus $$E[T\\mid U=u]=\\frac1{1-u},$$ and averaging over $u$ diverges: $$\\boxed{E[T]=\\infty}.$$ Also $P(T>k)=E[U^k]=1/(k+1)$, so $P(T\\le1)=1/2$. A median is $$\\boxed{1}.$$"}
{"name":"Frog Jumps","tag":["random-walk"],"statements":"A frog starts at the origin in the plane and makes three jumps. Each jump has length $1$, and each direction is independently uniform on $[0,2\\pi)$. What is the probability that after three jumps the frog is within distance $1$ of the origin?","hint":"Condition on the distance $r$ after the first two jumps, then ask for what angles of the third jump the endpoint falls in the unit disk.","solution":"Let $\\alpha\\in[0,\\pi]$ be the smaller angle between the first two jumps; by rotational symmetry $\\alpha$ is uniform. After two jumps the distance from the origin is $r=2\\cos(\\alpha/2)$. Given $r$, the third unit jump lands within distance $1$ iff its angle $\\theta$ from the current displacement satisfies $r^2+1+2r\\cos\\theta\\le1$, i.e. $\\cos\\theta\\le-r/2$. The conditional probability is $\\arccos(r/2)/\\pi=\\alpha/(2\\pi)$. Averaging gives $$E\\left[\\frac{\\alpha}{2\\pi}\\right]=\\boxed{\\frac14}.$$"}
{"name":"Dice And Coins","tag":["probability"],"statements":"Throw a fair die $100$ times and let $X$ be the sum. Flip a fair coin $600$ times and let $Y$ be the number of heads. Estimate $\\Pr(X>Y)$.","hint":"Use the normal approximation, or compute the exact distribution of the die sum by convolution and combine it with the binomial CDF.","solution":"Let $X$ be the sum of $100$ dice and $Y\\sim\\operatorname{Binomial}(600,1/2)$. Exact computation can be done by dynamic programming: $$P(X>Y)=\\sum_x P(X=x)P(Y<x).$$ A normal approximation gives $E[X-Y]=50$ and $\\operatorname{Var}(X-Y)=100\\cdot35/12+150=441.67$, so the z-score is about $2.38$. Thus $$\\boxed{P(X>Y)\\approx0.991}.$$"}
{"name":"Money Bomb Box","tag":["game"],"statements":"A box contains either $100$ dollars with probability $1/2$, or a bomb with probability $1/2$. If it contains a bomb, the explosion time is uniformly distributed over $1,2,\\dots,100$. Given that no explosion has occurred by time $n$, what is the fair price of the box? Follow-up: if the buyer has a $10\\%$ chance of knowing whether there is a bomb, what price should the seller charge?","hint":"Use Bayes' rule after conditioning on survival until time $n$.","solution":"Let $S_n$ be the event that no explosion has occurred by time $n$. Then $P(S_n\\mid\\text{money})=1$ and $P(S_n\\mid\\text{bomb})=(100-n)/100$. Bayes' rule gives $$P(\\text{money}\\mid S_n)=\\frac{1/2}{1/2+(1/2)(100-n)/100}=\\frac{100}{200-n}.$$ The fair price is $100$ times this probability: $$\\boxed{\\frac{10000}{200-n}}.$$"}
{"name":"Sum Of Uniforms","tag":["geometry"],"statements":"Draw i.i.d. $X_i\\sim U(0,1)$ until the partial sum first exceeds $1$. What is the expected number of draws? Equivalently, use $\\Pr(X_1+\\cdots+X_n\\le 1)$.","hint":"The volume of the simplex $\\{x_i\\ge0: \\sum_i x_i\\le1\\}$ inside the unit cube is $1/n!$.","solution":"Let $N$ be the first index such that $X_1+\\cdots+X_N>1$. Then $$P(N>n)=P(X_1+\\cdots+X_n\\le1)=\\frac1{n!},$$ the volume of the standard simplex in $[0,1]^n$. Therefore $$E[N]=\\sum_{n\\ge0}P(N>n)=\\sum_{n\\ge0}\\frac1{n!}=\\boxed{e}.$$"}
{"name":"M&M Testing","tag":["bayes"],"statements":"A person claims they can identify the color of M&M candies by taste. There are $5$ colors. They taste $3$ candies and get $2$ correct. What can you say about the claim? What if they taste $100$ and get $40$ correct?","hint":"Model random guessing as a binomial distribution with success probability $1/5$.","solution":"Under random guessing, $X\\sim\\operatorname{Binomial}(n,1/5)$. For $n=3$, $$P(X\\ge2)=\\binom32(1/5)^2(4/5)+(1/5)^3=0.104,$$ so this is weak evidence: $$\\boxed{\\text{not convincing}}.$$ For $n=100$ and $X=40$, the null mean is $20$ and the standard deviation is $4$; $40$ correct is about five standard deviations above the mean, giving an extremely small tail probability. This is $$\\boxed{\\text{strong evidence against random guessing}}.$$"}
{"name":"Commuting Matrices","tag":["linear-algebra"],"statements":"Which $n\\times n$ matrices commute with every $n\\times n$ matrix $B$, i.e. satisfy $AB=BA$ for all $B$?","hint":"First commute with diagonal matrices to force $A$ to be diagonal; then commute with elementary matrices $E_{ij}$ to force all diagonal entries to be equal.","solution":"If $A$ commutes with every diagonal matrix, then all off-diagonal entries of $A$ must be zero, so $A$ is diagonal. If $A=\\operatorname{diag}(a_1,\\dots,a_n)$ commutes with the matrix unit $E_{ij}$, then $AE_{ij}=a_iE_{ij}$ and $E_{ij}A=a_jE_{ij}$, so $a_i=a_j$ for all $i,j$. Hence the only such matrices are scalar multiples of the identity: $$\\boxed{A=cI}.$$"}
{"name":"Sample Until Decrease","tag":["stopping-time"],"statements":"Draw i.i.d. samples from a continuous distribution until the new sample is smaller than the previous sample. What is the expected number of samples, including the stopping sample? Does the answer depend on the distribution?","hint":"For continuous i.i.d. samples, all relative orderings of the first $k$ samples are equally likely.","solution":"The process has not stopped after $k$ samples iff $X_1<X_2<\\cdots<X_k$. For continuous iid samples, all $k!$ orderings are equally likely, so this has probability $1/k!$. If $N$ includes the stopping sample, then $$E[N]=\\sum_{k\\ge0}P(N>k)=1+\\sum_{k\\ge1}\\frac1{k!}=\\boxed{e}.$$ The result is distribution-free for continuous iid samples."}
{"name":"Dice Stopping Sum","tag":["stopping-time","dice"],"statements":"Roll a fair die until a specified face appears, including the final roll. What is the expected sum of all rolls? Does it depend on which face is the stopping face?","hint":"Use Wald's equation with the stopping time equal to a geometric random variable with parameter $1/6$.","solution":"Let the stopping face be $t$. There are $5$ expected non-target rolls before the first target, and a non-target roll has average value $(21-t)/5$. The final roll contributes $t$. Thus $$5\\cdot\\frac{21-t}{5}+t=21.$$ The expected sum is independent of $t$: $$\\boxed{21}.$$"}
{"name":"Uniform Disk Point","tag":["geometry"],"statements":"Given unlimited independent samples from $U(0,1)$, how can you simulate a uniform point in the unit disk? Can it be done from only one $U(0,1)$ sample?","hint":"The radius is not uniform; the disk area inside radius $r$ is proportional to $r^2$.","solution":"Using two independent uniforms $U,V$, set $$R=\\sqrt U,\\qquad \\Theta=2\\pi V,$$ and return $(R\\cos\\Theta,R\\sin\\Theta)$. Since $P(R\\le r)=P(U\\le r^2)=r^2$, the radial distribution is correct for area measure. The standard simulation answer is $$\\boxed{(\\sqrt U\\cos(2\\pi V),\\sqrt U\\sin(2\\pi V))}.$$ In theory one $U(0,1)$ sample can be split into two independent uniforms using alternating binary digits, ignoring the probability-zero dyadic ambiguity."}
{"name":"Balls Into Boxes","tag":["occupancy"],"statements":"There are three boxes: one contains two blue balls, one contains two red balls, and one contains one blue and one red ball. A box is chosen uniformly and one ball drawn is blue. What is the probability that the other ball in the same box is also blue?","hint":"Condition on observing a blue ball by counting equally likely blue-ball observations, not boxes.","solution":"Condition on the observed blue ball. Among all blue-ball observations, two come from the $BB$ box and one comes from the $BR$ box. Therefore the probability that the other ball is also blue is $$\\boxed{\\frac23}.$$"}
{"name":"Even Roll Expectation","tag":["conditional-expectation"],"statements":"What is the expected number of die rolls needed to get a $6$, conditioned on the event that all observed rolls are even?","hint":"After conditioning on even outcomes, each non-stopping roll is effectively sampled from $\\{2,4,6\\}$.","solution":"Conditioned on all observed rolls being even, each roll is uniformly distributed over $\\{2,4,6\\}$. The chance of rolling a $6$ on each conditioned roll is $1/3$, so the waiting time is geometric with parameter $1/3$. Therefore $$\\boxed{E[N]=3}.$$"}
{"name":"External Median","tag":["data-structures"],"statements":"A file contains $10^{12}$ elements and cannot fit in memory. How can you find the median? How would you estimate correctness if using sampling?","hint":"Separate exact external-memory selection from approximate sampling. Sampling alone gives a confidence interval, not a deterministic guarantee.","solution":"For an exact median, use an external-memory selection algorithm: choose a pivot, stream through the file to count elements below, equal to, and above it, then keep only the value range that can contain the median rank. Repeat until the median is determined. If keys are bounded integers, bucket or histogram passes can do the same more directly. Sampling can estimate a median with confidence bounds, but it is not an exact certificate unless followed by a counting pass. This is an algorithm-design problem rather than a single numeric answer."}
{"name":"Cards Before Ace","tag":["expectation","cards"],"statements":"In a shuffled standard $52$-card deck, what is the expected number of cards drawn before seeing the first ace? What if the count includes the first ace?","hint":"Insert the $48$ non-aces into the $5$ gaps around the $4$ aces.","solution":"The $48$ non-aces are symmetrically distributed among the $5$ gaps around the four aces: before the first ace, between consecutive aces, and after the last ace. The expected number of non-aces before the first ace is $$\\boxed{\\frac{48}{5}}.$$ If the count includes the first ace, add $1$: $$\\boxed{\\frac{53}{5}}.$$"}
{"name":"Coin Game","tag":["game"],"statements":"Player A flips $n+1$ fair coins and player B flips $n$ fair coins. What is the probability that A gets strictly more heads than B?","hint":"First compare the first $n$ coins of A with B's $n$ coins; symmetry handles ties.","solution":"Let $A'$ be A's heads among the first $n$ coins, and let $B$ be B's heads. By symmetry, $P(A'>B)=P(B>A')$. A wins if $A'>B$, and if $A'=B$ A wins exactly when the extra coin is heads. Hence $$P(A\\text{ wins})=P(A'>B)+\\frac12P(A'=B)=\\boxed{\\frac12}.$$"}
{"name":"Range Sum Structure","tag":["data-structures"],"statements":"Design data structures for range-sum queries. Start with a static one-dimensional array, then allow point updates. Extend to a two-dimensional matrix with rectangle-sum queries and point updates.","hint":"Static queries can be precomputed with prefix sums; updates require a tree-like structure.","solution":"For a static 1D array, use prefix sums so a range query is answered in $O(1)$. With point updates, use a Fenwick tree or segment tree for $O(\\log n)$ update and query. For a static 2D matrix, use a 2D prefix sum with inclusion-exclusion. With 2D point updates, use a 2D Fenwick tree or segment tree. The standard structures are $$\\boxed{\\text{prefix sums / Fenwick trees / segment trees}}.$$"}
{"name":"XOR Coins","tag":["simulation"],"statements":"You have coins represented as Bernoulli random variables. What happens when you XOR: (1) a fair coin with a double-headed coin, (2) two coins with $p=0.4$, (3) infinitely many coins with $p=0.4$, and (4) any number of biased coins plus one fair coin? How can a biased coin generate a fair coin?","hint":"For independent Bernoulli variables, XOR means parity. Track the probability of parity $1$ recursively.","solution":"Treat heads as $1$ and tails as $0$. XOR with a deterministic double-headed coin just flips a fair bit, so it remains $$\\boxed{\\text{fair}}.$$ For two independent coins with $P(1)=p=0.4$, $$P(\\text{XOR}=1)=2p(1-p)=\\boxed{0.48}.$$ For $n$ such coins, $P(\\text{odd parity})=(1-(1-2p)^n)/2$, which tends to $$\\boxed{1/2}.$$ XORing any independent collection with one fair coin is also $$\\boxed{\\text{fair}}.$$ To extract a fair bit from a biased coin, use von Neumann: output one value on $HT$, the other on $TH$, and ignore $HH,TT$."}
{"name":"Stick Triangle","tag":["geometry"],"statements":"A stick is cut at two independent uniformly random points. What is the probability that the three resulting segments can form a triangle?","hint":"The three pieces form a triangle iff no piece is longer than half the stick.","solution":"Let the ordered cuts be $0<X<Y<1$. The segment lengths are $X$, $Y-X$, and $1-Y$. They form a triangle iff all three are less than $1/2$. The favorable region in the unit square of cut points has area $1/4$, so the probability is $$\\boxed{\\frac14}.$$"}
{"name":"Painted Cube","tag":["combinatorics"],"statements":"A $3\\times3\\times3$ white cube is painted red on the outside and then cut into $27$ unit cubes. One small cube is chosen uniformly at random and oriented uniformly at random. You are shown only the top face, and it is white. What is the probability that the bottom face is red?","hint":"After observing only white visible faces, the cube is either the center cube or a face-center cube with exactly one red face. Use Bayes' rule.","solution":"Conditioning on seeing a white top face is equivalent to choosing uniformly among all white faces of all small cubes as the top. Across the $27$ cubes there are $27\\cdot6-6\\cdot9=108$ white faces. Every red exterior face has a white opposite face, and there are $54$ red exterior faces, so there are $54$ favorable orientations with white top and red bottom. The probability is $$\\boxed{\\frac{54}{108}=\\frac12}.$$"}
{"name":"K-Sorted Array","tag":["sorting"],"statements":"An array of length $n$ is almost sorted: every element is at most $k$ positions away from its correct sorted position, with $k\\ll n$. How can you sort it efficiently?","hint":"The smallest remaining element must be among the next $k+1$ candidates.","solution":"Maintain a min-heap containing the next $k+1$ elements. Repeatedly pop the minimum into the output and push the next unseen element. Because every element is at most $k$ positions away from its sorted location, the next output element must be in that heap. The runtime is $$\\boxed{O(n\\log k)}$$ and the extra memory is $$\\boxed{O(k)}.$$"}
{"name":"Red Black Runs","tag":["runs"],"statements":"There are $n$ red cards and $n$ black cards in random order. Consecutive cards of the same color form one run. What is the expected number of runs? As a special case, what about $2$ red and $2$ black cards?","hint":"A new run starts at position $i>1$ exactly when card $i$ has a different color from card $i-1$.","solution":"Let $I_i$ indicate a color change between positions $i-1$ and $i$. For $i>1$, $$P(I_i=1)=\\frac{2n^2}{(2n)(2n-1)}=\\frac{n}{2n-1}.$$ The number of runs is $1+\\sum_{i=2}^{2n}I_i$, so $$E[\\text{runs}]=1+(2n-1)\\frac{n}{2n-1}=\\boxed{n+1}.$$ For $n=2$, this is $$\\boxed{3}.$$"}
{"name":"Noodle Loops","tag":["combinatorics"],"statements":"There are $n$ noodles in a bowl, so $2n$ loose ends. Repeatedly choose two loose ends uniformly at random and tie them together until no loose ends remain. What is the expected number of loops formed?","hint":"When there are $k$ open chains/noodles left, the probability that the next tie closes a loop is $1/(2k-1)$.","solution":"When there are $k$ noodle components with two loose ends each, choose one loose end. Among the remaining $2k-1$ loose ends, exactly one is the other end of the same component and would close a loop. The expected loop contribution at that stage is $1/(2k-1)$. Summing over $k=n,n-1,\\ldots,1$ gives $$\\boxed{\\sum_{k=1}^n\\frac1{2k-1}}.$$"}
{"name":"Gaussian Angle","tag":["normal-distribution","geometry"],"statements":"Let $X,Y$ be independent standard normal random variables. Find $\\Pr(Y>3X)$. Follow-up: find $\\Pr(Y>3X\\mid X>0)$.","hint":"Use rotational symmetry of the bivariate standard normal distribution.","solution":"Unconditionally, $Y-3X$ is a centered normal random variable, so $$\\boxed{P(Y>3X)=\\frac12}.$$ Conditional on $X>0$, the polar angle is uniform over an interval of length $\\pi$. The event $Y>3X$ in the right half-plane has angular length $\\pi/2-\\arctan 3=\\arctan(1/3)$, so $$\\boxed{P(Y>3X\\mid X>0)=\\frac{\\arctan(1/3)}{\\pi}}.$$"}
{"name":"Kelly Bet","tag":["optimization"],"statements":"You have bankroll $1000$. On each bet, wagering $x$ wins $2x$ profit with probability $p>1/2$ and loses $x$ with probability $1-p$. How much should you bet to maximize expected log utility?","hint":"Maximize $p\\log(W+2x)+(1-p)\\log(W-x)$ over $0\\le x\\le W$.","solution":"Let $f=x/1000$. The expected log growth is $$g(f)=p\\log(1+2f)+(1-p)\\log(1-f).$$ Differentiate: $$g'(f)=\\frac{2p}{1+2f}-\\frac{1-p}{1-f}.$$ Setting $g'(f)=0$ gives $2p(1-f)=(1-p)(1+2f)$, hence $$f=\\frac{3p-1}{2}.$$ Under the stated $p>1/2$, this is interior, so the optimal bet is $$\\boxed{x=1000\\cdot\\frac{3p-1}{2}}.$$"}
{"name":"HH HT Posterior","tag":["bayes"],"statements":"There are $1000$ coins. One coin is double-headed ($HH$), and the other $999$ are fair or $HT$ coins. A coin is chosen uniformly and tossed repeatedly. If the first $10$ tosses are all heads, what is the posterior probability that the coin is $HH$? What if only the first $2$ tosses are heads?","hint":"Apply Bayes' rule. The double-headed coin always produces heads; a fair coin produces $k$ heads with probability $2^{-k}$.","solution":"After observing $k$ heads in a row, Bayes' rule gives $$P(HH\\mid kH)=\\frac{1\\cdot1/1000}{1/1000+2^{-k}\\cdot999/1000}=\\boxed{\\frac{1}{1+999/2^k}}.$$ For $k=10$, this is $$\\boxed{\\frac{1024}{2023}\\approx0.506}.$$ For $k=2$, this is $$\\boxed{\\frac4{1003}}.$$"}
{"name":"Children Puzzle","tag":["conditional-probability"],"statements":"You have three children. What is the probability that at least two are girls?","hint":"Count outcomes among $2^3$ equally likely gender sequences.","solution":"There are $2^3=8$ equally likely gender sequences. The outcomes with at least two girls are $\\binom32+\\binom33=4$. Therefore the probability is $$\\boxed{\\frac48=\\frac12}.$$"}
{"name":"Grid Guards","tag":["grid"],"statements":"Given a grid with obstacles, guards, a start, and a target, decide whether there is a path from start to target. Guards can see cells in front of them, so those watched cells are unsafe.","hint":"Precompute blocked/watched cells, then run BFS/DFS on the remaining grid.","solution":"First mark walls and obstacles as blocked. For each guard, ray-cast in its facing direction until hitting an obstacle or boundary, marking all visible cells as unsafe. Then run BFS or DFS from the start over cells that are neither blocked nor watched. The target is reachable iff this search reaches it. This is an algorithmic reachability problem; a specific submitted answer depends on the input grid."}
{"name":"Drunk Passenger","tag":["symmetry"],"statements":"A line of $n$ passengers boards a plane with seats labeled $1$ through $n$. Passenger $1$ ignores their ticket and chooses a random seat. Every later passenger takes their assigned seat if available; otherwise they choose a random empty seat. What is the probability that passenger $n$ gets seat $n$?","hint":"Only the first and last seats matter once the process starts displacing people.","solution":"Track the first time either seat $1$ or seat $n$ is taken by a displaced passenger. If seat $1$ is taken first, every later passenger gets their own seat, including passenger $n$. If seat $n$ is taken first, passenger $n$ loses their seat. By symmetry these two seats are equally likely to be the first of the pair chosen, so the probability is $$\\boxed{\\frac12}.$$"}
{"name":"Points In Semicircle","tag":["geometry"],"statements":"Choose $n$ points independently and uniformly on the circumference of a circle. What is the probability that all of them lie inside some semicircle?","hint":"Fix one chosen point as the clockwise start of a candidate semicircle.","solution":"For any fixed point, the probability that all other $n-1$ points fall in the clockwise semicircle starting there is $(1/2)^{n-1}$. For $n\\ge2$, the events for different starting points are disjoint except on probability-zero boundary cases. Therefore the probability is $$\\boxed{n\\left(\\frac12\\right)^{n-1}=\\frac{n}{2^{n-1}}}.$$"}
{"name":"Birthday Threshold","tag":["combinatorics"],"statements":"Assume birthdays are independent and uniformly distributed across $365$ days. What is the smallest class size for which the probability of at least one shared birthday exceeds $1/2$?","hint":"Compute the complement: no two birthdays match.","solution":"For $n$ people, $$P(\\text{all distinct})=\\frac{365\\cdot364\\cdots(365-n+1)}{365^n}.$$ The shared-birthday probability is one minus this value. Checking the threshold gives $P(\\text{match})<1/2$ for $n=22$ and $P(\\text{match})>1/2$ for $n=23$, so the answer is $$\\boxed{23}.$$"}
{"name":"Increasing Dice","tag":["dice"],"statements":"Roll three fair dice one after another. What is the probability that the three observed numbers are in strictly increasing order?","hint":"First require the three values to be distinct; then use symmetry over their orderings.","solution":"Choose the three distinct face values, then put them in increasing order. There are $\\binom63$ favorable sequences out of $6^3$ total rolls. Thus $$\\boxed{\\frac{\\binom63}{6^3}=\\frac{5}{54}}.$$"}
{"name":"Monty Hall","tag":["conditional-probability"],"statements":"In a Monty Hall game, one of three doors hides a prize and the other two hide goats. You choose one door; the host, who knows the locations, opens a different goat door and offers you the chance to switch. Should you switch, and what is the winning probability if you do?","hint":"Switching wins exactly when your first choice was wrong.","solution":"Your original door has probability $1/3$ of hiding the prize. Switching wins exactly when the original choice was one of the two goat doors, which has probability $2/3$. Therefore the correct strategy is $$\\boxed{\\text{switch}},$$ with winning probability $$\\boxed{\\frac23}.$$"}
{"name":"Amoeba Extinction","tag":["branching-process"],"statements":"A population starts with one amoeba. Each minute, every amoeba independently either dies, remains one amoeba, splits into two, or splits into three, with equal probabilities. What is the probability that the population eventually dies out?","hint":"Let $x$ be the extinction probability and condition on the first minute.","solution":"Let $x$ be the extinction probability starting from one amoeba. Conditioning on the first minute gives $$x=\\frac14(1+x+x^2+x^3).$$ Thus $x^3+x^2-3x+1=0=(x-1)(x^2+2x-1)$. The valid root in $[0,1)$ is $$\\boxed{\\sqrt2-1}.$$"}
{"name":"Russian Roulette Order","tag":["game"],"statements":"A six-chamber revolver has one bullet. The cylinder is spun once, then two players alternate trigger pulls without spinning again until someone loses. If you may choose to go first or second, which should you choose?","hint":"After the initial spin, the bullet position is fixed.","solution":"After the initial spin, the bullet position is fixed. If you go first, you lose when the bullet is in chamber $1$, $3$, or $5$; if you go second, you lose when it is in chamber $2$, $4$, or $6$. These have equal probability. Therefore the choice does not matter, and the loss probability is $$\\boxed{\\frac12}.$$"}
{"name":"Adjacent Bullets","tag":["conditional-probability","game"],"statements":"A six-chamber revolver has two adjacent bullets. Your opponent pulls first and survives. You must pull next. Should you spin the cylinder before pulling?","hint":"Condition on which empty chamber the opponent just hit.","solution":"Given that the opponent survived, their chamber was one of the four empty chambers, equally likely. With adjacent bullets, exactly one of those four empty chambers is immediately followed by a bullet, so not spinning gives death probability $1/4$. Spinning gives death probability $2/6=1/3$. Since $1/4<1/3$, the answer is $$\\boxed{\\text{do not spin}},$$ with death probability $$\\boxed{\\frac14}.$$"}
{"name":"Gambler Ruin","tag":["random-walk"],"statements":"A gambler starts with $i$ dollars and stops when their fortune hits either $0$ or $N$. Each round they win $1$ with probability $p$ and lose $1$ with probability $q=1-p$. What is the probability of reaching $N$ before $0$?","hint":"Write a recurrence for $u_i=P_i(\\text{hit }N\\text{ before }0)$ with boundary values $u_0=0$ and $u_N=1$.","solution":"Let $u_i$ be the probability of hitting $N$ before $0$ starting from $i$. The recurrence is $u_i=pu_{i+1}+qu_{i-1}$ with $u_0=0$ and $u_N=1$. Solving gives $$\\boxed{u_i=\\frac{1-(q/p)^i}{1-(q/p)^N}\\quad(p\\ne q)}.$$ In the fair case, $$\\boxed{u_i=\\frac{i}{N}}.$$"}
{"name":"Station Meeting","tag":["geometry"],"statements":"Two people independently arrive at a station at uniformly random times during a $T$-minute window. Each waits exactly $w$ minutes and then leaves, where $0\\le w\\le T$. What is the probability that they meet?","hint":"Draw the square of arrival times and remove the two triangles where their arrival times differ by more than $w$.","solution":"Let the arrival times be $X,Y\\sim U[0,T]$. They meet iff $|X-Y|\\le w$. The non-meeting region consists of two triangles with total area $(T-w)^2$, so $$\\boxed{P(\\text{meet})=1-\\left(1-\\frac{w}{T}\\right)^2}.$$ For $T=60,w=5$, this is $$\\boxed{\\frac{23}{144}}.$$"}
{"name":"Poisson Bus Wait","tag":["poisson-process"],"statements":"Buses arrive according to a Poisson process with rate $\\lambda$ per minute. You arrive at a random time after the process has been running for a long time. What is your expected waiting time until the next bus, and what is the expected time since the previous bus?","hint":"Use the memoryless property of exponential interarrival times.","solution":"Poisson interarrival times are exponential with mean $1/\\lambda$. By memorylessness, the residual waiting time from a random observation time has the same mean, and the backward recurrence time has the same distribution. Therefore both expectations are $$\\boxed{\\frac1\\lambda}.$$ For $\\lambda=0.1$ per minute, both are $$\\boxed{10\\text{ minutes}}.$$"}
{"name":"Normal Moments","tag":["normal-distribution"],"statements":"Let $X$ be a standard normal random variable. Compute $E[X]$, $E[X^2]$, $E[X^3]$, and $E[X^4]$.","hint":"Odd moments vanish by symmetry; even moments can be derived by integration by parts or the moment generating function.","solution":"The standard normal distribution is symmetric, so $E[X]=E[X^3]=0$. Its variance is $1$, so $E[X^2]=1$. Using $M_X(t)=e^{t^2/2}$ or integration by parts gives $E[X^4]=3$. Therefore $$\\boxed{(E[X],E[X^2],E[X^3],E[X^4])=(0,1,0,3)}.$$"}
{"name":"Coupon Collector","tag":["coupon-collector"],"statements":"There are $N$ equally likely coupon types. Each box contains one independent random coupon. What is the expected number of boxes needed to collect all $N$ types? Also, after opening $n$ boxes, what is the expected number of distinct types seen?","hint":"For the first part, decompose the time to get the next new type. For the second, use indicators for whether each type has appeared.","solution":"After $k$ distinct types have been collected, the chance that the next box gives a new type is $(N-k)/N$, so the expected additional wait is $N/(N-k)$. Hence $$\\boxed{E[T]=\\sum_{k=0}^{N-1}\\frac{N}{N-k}=N H_N}.$$ For the number of distinct types after $n$ boxes, indicator $I_j$ for type $j$ appearing has expectation $1-(1-1/N)^n$. By linearity, $$\\boxed{E[D_n]=N\\left(1-\\left(1-\\frac1N\\right)^n\\right)}.$$"}
{"name":"Uniform Extremes","tag":["order-statistics"],"statements":"Let $X_1,\\ldots,X_n$ be iid $U[0,1]$. Find the CDF, density, and expectation of $M=\\max_i X_i$ and $m=\\min_i X_i$.","hint":"For the max, all samples must be below $x$; for the min, all samples must be above $x$.","solution":"For $0\\le x\\le1$, $$\\boxed{F_M(x)=P(M\\le x)=x^n},\\qquad \\boxed{f_M(x)=n x^{n-1}},\\qquad \\boxed{E[M]=\\frac{n}{n+1}}.$$ Also $P(m>x)=(1-x)^n$, so $$\\boxed{F_m(x)=1-(1-x)^n},\\qquad \\boxed{f_m(x)=n(1-x)^{n-1}},\\qquad \\boxed{E[m]=\\frac1{n+1}}.$$"}
{"name":"Falling Ants","tag":["order-statistics","symmetry"],"statements":"$n$ ants are placed independently and uniformly on a stick of length $L$. Each ant independently chooses left or right with equal probability and walks at speed $v$. When two ants collide they reverse directions. What is the expected time until all ants fall off?","hint":"A collision with label-swapping is equivalent to the ants passing through each other.","solution":"When two ants collide and reverse, the unlabeled motion is equivalent to the ants passing through each other. For each ant, the distance to its chosen endpoint is uniform on $[0,L]$. The finishing time is the maximum of $n$ iid $U[0,L]$ variables divided by $v$. Therefore $$\\boxed{E[T]=\\frac{nL}{(n+1)v}}.$$"}
{"name":"Fisher Yates Shuffle","tag":["randomization"],"statements":"You have a random integer generator that can sample uniformly from any interval $[i,j]$. How do you shuffle an array of $n$ distinct items so that every permutation is equally likely?","hint":"Choose the first item uniformly, then choose the second from the remaining items, and so on.","solution":"Use Fisher-Yates: for $i=1,2,\\ldots,n$, choose $j$ uniformly from $\\{i,i+1,\\ldots,n\\}$ and swap $A_i$ with $A_j$. At step $i$, every remaining item is equally likely to be placed in position $i$. Therefore each final ordering has probability $1/n!$. The standard answer is $$\\boxed{\\text{Fisher-Yates shuffle}}.$$"}
{"name":"Reservoir Sample Char","tag":["streaming"],"statements":"A file is streamed character by character, and its length is unknown in advance. Design an algorithm that returns one character uniformly at random from the entire file using $O(1)$ memory.","hint":"When the $k$-th character arrives, give it probability $1/k$ of replacing the current sample.","solution":"Keep one sampled character. Store the first character. When character $k$ arrives, replace the stored character with probability $1/k$. By induction, after processing $k$ characters each has probability $1/k$ of being stored. The update rule is $$\\boxed{\\text{replace on item }k\\text{ with probability }1/k},$$ which yields a uniform final sample."}
{"name":"Min Max Comparisons","tag":["comparisons"],"statements":"Given $n$ unsorted numbers, find both the minimum and maximum using at most about $3n/2$ comparisons.","hint":"Compare numbers in pairs first so each element only competes in the min side or the max side.","solution":"Pair up the elements and compare within each pair. The smaller elements are the only candidates for the global minimum, and the larger elements are the only candidates for the global maximum. This costs $n/2$ pair comparisons, then $n/2-1$ comparisons for the min side and $n/2-1$ for the max side. For even $n$, the total is $$\\boxed{\\frac{3n}{2}-2}.$$"}
{"name":"First Nonzero Search","tag":["binary-search"],"statements":"An array has an unknown length. It consists of some number of leading zeros followed by only nonzero values. How can you find the index of the first nonzero value efficiently?","hint":"First bracket the transition point by checking indices that double each time.","solution":"Probe indices $1,2,4,8,\\ldots$ until finding a nonzero value, thereby bracketing the first nonzero index. Then binary-search inside the bracket. If the first nonzero position is $m$, the number of probes is $$\\boxed{O(\\log m)}.$$"}
{"name":"Max Subarray Sum","tag":["prefix-sum"],"statements":"Given an array $A_1,\\ldots,A_n$ containing positive and negative numbers, design an $O(n)$ algorithm to find the maximum sum of a contiguous subarray.","hint":"For a subarray ending at position $j$, subtract the smallest prefix sum before $j$.","solution":"Let $S_j=\\sum_{k=1}^j A_k$ and $S_0=0$. The best subarray ending at $j$ has value $S_j-\\min_{0\\le t<j}S_t$. Scan left to right, maintaining the current prefix sum, the smallest prefix sum seen so far, and the best difference. This gives the maximum contiguous subarray sum in $$\\boxed{O(n)}$$ time and $O(1)$ extra space."}
{"name":"Simulate Probability","tag":["simulation"],"statements":"You have a fair coin and want to design a game whose winning probability is an arbitrary fixed real number $p\\in(0,1)$. How can you do it?","hint":"Compare fair-coin bits to the binary expansion of $p$.","solution":"Write $p$ in binary as $p=0.p_1p_2p_3\\ldots$. Toss the fair coin repeatedly to generate bits $b_1,b_2,\\ldots$, which define $U\\sim U[0,1]$. At the first index where $b_i\\ne p_i$, win if $b_i<p_i$ and lose if $b_i>p_i$. Equivalently, win iff $U<p$, so $$\\boxed{P(\\text{win})=p}.$$"}
{"name":"Poisoned Bottle Coding","tag":["bitmask"],"statements":"There are $1000$ bottles and exactly one is poisoned. You have $10$ test animals; any animal that drinks any poison dies exactly $18$ hours later. You have only enough time for one simultaneous testing round. Can you identify the poisoned bottle with certainty?","hint":"$10$ binary outcomes can distinguish up to $2^{10}$ cases.","solution":"Label the bottles from $0$ to $999$ in binary using $10$ bits. For bit position $k$, give animal $k$ a sample from every bottle whose $k$-th bit is $1$. After $18$ hours, the dead/live pattern is exactly the binary label of the poisoned bottle. Since $2^{10}=1024\\ge1000$, the answer is $$\\boxed{\\text{yes, 10 animals are enough}}.$$"}
{"name":"Polya Free Throws","tag":["polya-urn"],"statements":"A basketball player makes the first free throw and misses the second. For every later throw, her probability of making it equals her current fraction of made shots. After $100$ total throws, what is the probability that she has made exactly $50$ shots?","hint":"Look at the distribution after $n$ throws for small $n$; it becomes uniform over the possible made-shot counts.","solution":"This is Polya's urn with one initial make and one initial miss. After $n$ throws, the number of makes is uniformly distributed over $1,2,\\ldots,n-1$; this follows by induction from the transition probabilities $k/n$ and $(n-k)/n$. Therefore after $100$ throws, each count from $1$ to $99$ has probability $1/99$, so the desired probability is $$\\boxed{\\frac1{99}}.$$"}
